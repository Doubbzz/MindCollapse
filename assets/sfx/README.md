# Audio Placeholders

This project uses procedurally generated audio at runtime so no binary sound files are stored in the repository. You can drop your own `.ogg` or `.wav` files in this folder and wire them up in the scenes if you prefer custom effects.
